# Copia este archivo a .env.local (ignorado por git) y coloca tu token público de Mapbox.
# Se obtiene en https://account.mapbox.com/access-tokens/ (el token "Default public token" sirve).
VITE_MAPBOX_TOKEN=pk.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
